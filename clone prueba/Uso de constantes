// En una validación de servicio
if (calificacion < ReglasDominio.CalificacionMinima ||
    calificacion > ReglasDominio.CalificacionMaxima)
{
    throw new ArgumentException(
        "Calificación fuera de rango.");
}

// En Data Annotations (semana 6)
[MaxLength(ReglasDominio.NombreMaxLongitud)]
public string Nombre { get; set; } = string.Empty;
    